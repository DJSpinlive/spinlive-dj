import BookingsView from "@/components/studio/bookings/BookingsView";

export const metadata = { title: "Bookings — SpinLive Studio" };

export default function BookingsPage() {
  return <BookingsView />;
}
