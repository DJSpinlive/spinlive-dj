import CalendarView from "@/components/studio/calendar/CalendarView";

export const metadata = { title: "Calendar — SpinLive Studio" };

export default function CalendarPage() {
  return <CalendarView />;
}
