import SettingsView from "@/components/studio/settings/SettingsView";

export const metadata = { title: "Settings — SpinLive Studio" };

export default function SettingsPage() {
  return <SettingsView />;
}
