import EarningsView from "@/components/studio/earnings/EarningsView";

export const metadata = { title: "Earnings — SpinLive Studio" };

export default function EarningsPage() {
  return <EarningsView />;
}
