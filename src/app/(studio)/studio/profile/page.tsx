import ProfileView from "@/components/studio/profile/ProfileView";

export const metadata = { title: "Profile — SpinLive Studio" };

export default function ProfilePage() {
  return <ProfileView />;
}
